# v2.14 — Hotfix de inicialização no iPhone

Status: **PUBLICADA EM PRODUÇÃO**.

## Relato real
No iPhone/Safari, a v2.13 exibiu `Não foi possível iniciar o sistema` com `JSON Parse error: Invalid escape character e` antes do login. O logo também aparecia como uma cápsula branca vazia.

## Diagnóstico
- O snapshot `runtime-v213` foi reconstruído e o JSON compactado foi validado fora do navegador; o pacote em si é íntegro.
- O erro observado é compatível com uma sessão Supabase persistida inválida/corrompida em `localStorage`, capaz de disparar `JSON.parse` interno antes da renderização.
- A v2.13 não fazia validação preventiva dessa sessão, então o erro derrubava todo o boot e aparecia como falha genérica.

## Correção
- Antes de iniciar o app, validar somente `sb-kedggjyerexnzmipaick-auth-token`.
- Se esse token existir e não for JSON válido, remover **somente** a sessão local corrompida. Nenhuma empresa, arte, orçamento, mídia ou dado de banco é apagado; no máximo o aparelho precisará efetuar login novamente.
- Se um erro de JSON escapar durante a primeira execução, limpar o mesmo token e recarregar uma única vez com guarda em `sessionStorage`, evitando loop.
- Logo oficial agora é servido como `/zero19-logo.svg` autocontido no mesmo deployment, sem depender do Raw GitHub para aparecer.
- Interface interna mostra `v2.14`; páginas públicas não exibem versão.

## Não regressão
Todos os módulos da v2.13 permanecem preservados: catálogo/qualidades, portfólio, 100 MB, 1 vídeo + 3 fotos, curtidas, comentários, templates WhatsApp, integração com produtos/orçamento, estampas, criação de arte, comissões e divisão de comissão.

## Publicação
- Deployment Vercel: `dpl_8RaTFdAs1yjkF3e4Gi3P4ZENCfv7`.
- Target: `production`.
- Estado: `READY`.
- Aliases: `019-personalizacoes.vercel.app` e `019-personalizacoes-uniquess.vercel.app`.
- Domínio canônico confirmado HTTP 200 com `z19-version="2.14"`.
- `/zero19-logo.svg` confirmado HTTP 200, autocontido.
- `/qualidades.html`, `/portfolio.html`, `/comercial-admin.html` e `/demo.html` confirmados HTTP 200.

## Regra adicionada
Smoke test de produção deve validar o boot e o estado de sessão, não apenas HTTP 200/READY. Erro de sessão local nunca pode derrubar o aplicativo inteiro.
