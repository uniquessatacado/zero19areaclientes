# v2.15 — Recuperação adicional do boot no Safari

Status: **PUBLICADA EM PRODUÇÃO**.

## Problema observado
No iPhone/Safari, o sistema podia parar antes do login exibindo `JSON Parse error: Invalid escape character e`.

## Correção aplicada
- O boot valida e remove apenas a chave local de autenticação `sb-kedggjyerexnzmipaick-auth-token` quando ela não contém JSON válido.
- O carregador da release tenta `JSON.parse` e possui fallback controlado para o snapshot v2.13, sem usar `DecompressionStream`.
- A versão exibida internamente passou para **v2.15**.
- O domínio canônico foi conferido servindo `z19-version="2.15"`.

## Produção confirmada
- Projeto: `019-personalizacoes`
- Domínio: `https://019-personalizacoes.vercel.app`
- Deployment atual: `dpl_G592z8rFCnKbC8xwBmtEQwNmuBD2`
- Estado: `READY`
- Target: `production`

## Prevenção
Nunca considerar um boot validado apenas porque o deployment está READY. Conferir a versão servida pelo domínio e manter tratamento de sessão local inválida antes de iniciar o cliente Supabase.
